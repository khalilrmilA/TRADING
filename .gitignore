# Environments & caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local data & runtime state (SQLite DB, logs, PIDs)
data/
*.log
tmp_uvicorn_pid.txt
pytest_out.txt

# Local configuration (keep .env.example tracked)
.env
